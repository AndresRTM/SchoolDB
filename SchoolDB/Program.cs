using Azure.Core;
using SchoolDB.Data;
namespace SchoolDB
{
    internal class Program
    {
        static void Main(string[] args)
        {
            using var context = new SchoolDBContext();

        }

        static void RunMenu()
        {
            bool isRunning = true;
            while (isRunning)
            {
                switch (switch_on)
                {

                    default:
                }
            }
        }
    }
}